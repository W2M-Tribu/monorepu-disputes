spring:
  application:
    name: w2m-disputes

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: w2m-disputes-service
            client-secret: 4oGTiN1LTLZHGegE0X8Uuh9IMsRe7gFI
            authorization-grant-type: client_credentials
            provider: keycloak
        provider:
          keycloak:
            token-uri: http://localhost:8081/realms/w2m-disputes/protocol/openid-connect/token

  datasource:
    url: jdbc:postgresql://localhost:5432/disputesdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8090
